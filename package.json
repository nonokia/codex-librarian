{
  "name": "codex-librarian",
  "version": "0.1.0",
  "description": "Comprehension-debt repayment: graph-first code review & knowledge platform (see docs/architecture.md)",
  "type": "module",
  "license": "MIT",
  "bin": {
    "librarian": "bin/librarian.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test \"dist/test/*.test.js\"",
    "selfindex": "npm run build && node scripts/selfindex.mjs",
    "selfindex:check": "npm run build && node scripts/selfindex.mjs --check"
  },
  "engines": {
    "node": ">=22.5"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.111.0",
    "@bufbuild/protobuf": "^2.12.1",
    "@scip-code/scip": "^0.9.0",
    "typescript": "^5.6.0"
  },
  "devDependencies": {
    "@types/node": "^22.20.1"
  }
}
